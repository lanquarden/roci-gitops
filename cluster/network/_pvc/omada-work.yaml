---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omada-work
  namespace: network
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: rook-ceph-block
