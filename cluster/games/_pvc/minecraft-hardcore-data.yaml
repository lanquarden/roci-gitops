---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-hardcore-data
  namespace: home
spec:
  accessModes:
  - ReadWriteOnce

  resources:
    requests:
      storage: 5Gi

  storageClassName: rook-ceph-block
